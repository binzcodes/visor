{
  "name": "visor",
  "version": "0.1.0",
  "description": "Full-coverage `<head>` gear for Astro apps.",
  "license": "MIT",
  "scripts": {
    "build": "astro build",
    "lint": "eslint",
    "format": "format"
  },
  "dependencies": {
    "@types/node": "^20.11.24",
    "astro": "^4.7.0",
    "web-vitals": "^3.5.2"
  },
  "devDependencies": {
    "@binz/eslint": "*"
  }
}
